{
  "name": "@bd/db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:apply": "npm run db:apply --workspace=@bd/api",
    "db:apply:remote": "npm run db:apply:remote --workspace=@bd/api",
    "db:seed": "npm run db:seed --workspace=@bd/api",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.7.3"
  }
}
